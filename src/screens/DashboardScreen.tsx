import React, { useState, useEffect } from 'react';\nimport {\n  View,\n  Text,\n  StyleSheet,\n  ScrollView,\n  TouchableOpacity,\n  ActivityIndicator,\n  RefreshControl,\n} from 'react-native';\nimport { useAuth } from '../contexts/AuthContext';\nimport { useWorkspace } from '../contexts/WorkspaceContext';\nimport { apiService } from '../services/api';\nimport { COLORS } from '../constants';\n\ninterface DashboardData {\n  totalDeudores: number;\n  deudaTotal: number;\n  pagosProximos: number;\n  pagosVencidos: number;\n  deudasPorDeudor: any[];\n}\n\nconst DashboardScreen: React.FC<{ navigation: any }> = ({ navigation }) => {\n  const { user } = useAuth();\n  const { workspace } = useWorkspace();\n  const [dashboardData, setDashboardData] = useState<DashboardData | null>(null);\n  const [isLoading, setIsLoading] = useState(true);\n  const [refreshing, setRefreshing] = useState(false);\n\n  const loadDashboardData = async () => {\n    if (!workspace) return;\n\n    try {\n      const response = await apiService.getReportes(workspace.id, {\n        type: 'dashboard',\n      });\n\n      if (response.success) {\n        setDashboardData(response.data);\n      }\n    } catch (error) {\n      console.error('Error loading dashboard:', error);\n    } finally {\n      setIsLoading(false);\n      setRefreshing(false);\n    }\n  };\n\n  useEffect(() => {\n    loadDashboardData();\n  }, [workspace]);\n\n  const onRefresh = () => {\n    setRefreshing(true);\n    loadDashboardData();\n  };\n\n  if (isLoading) {\n    return (\n      <View style={styles.centerContainer}>\n        <ActivityIndicator size=\"large\" color={COLORS.primary} />\n      </View>\n    );\n  }\n\n  return (\n    <ScrollView\n      style={styles.container}\n      refreshControl={<RefreshControl refreshing={refreshing} onRefresh={onRefresh} />}\n    >\n      <View style={styles.header}>\n        <Text style={styles.greeting}>Hola, {user?.name}</Text>\n        <Text style={styles.date}>{new Date().toLocaleDateString('es-ES')}</Text>\n      </View>\n\n      {/* Tarjetas de resumen */}\n      <View style={styles.summaryCards}>\n        <View style={[styles.card, styles.cardPrimary]}>\n          <Text style={styles.cardLabel}>Deuda Total</Text>\n          <Text style={styles.cardValue}>\n            ${dashboardData?.deudaTotal?.toLocaleString('es-CL') || '0'}\n          </Text>\n        </View>\n\n        <View style={[styles.card, styles.cardSecondary]}>\n          <Text style={styles.cardLabel}>Deudores</Text>\n          <Text style={styles.cardValue}>{dashboardData?.totalDeudores || 0}</Text>\n        </View>\n      </View>\n\n      {/* Alertas */}\n      <View style={styles.section}>\n        <Text style={styles.sectionTitle}>Alertas</Text>\n\n        {dashboardData && dashboardData.pagosVencidos > 0 && (\n          <View style={[styles.alert, styles.alertDanger]}>\n            <Text style={styles.alertTitle}>⚠️ Pagos Vencidos</Text>\n            <Text style={styles.alertText}>\n              Tienes {dashboardData.pagosVencidos} pago(s) vencido(s)\n            </Text>\n          </View>\n        )}\n\n        {dashboardData && dashboardData.pagosProximos > 0 && (\n          <View style={[styles.alert, styles.alertWarning]}>\n            <Text style={styles.alertTitle}>📅 Próximos Vencimientos</Text>\n            <Text style={styles.alertText}>\n              {dashboardData.pagosProximos} pago(s) vencerá(n) en los próximos 3 días\n            </Text>\n          </View>\n        )}\n\n        {!dashboardData || (dashboardData.pagosVencidos === 0 && dashboardData.pagosProximos === 0) && (\n          <View style={[styles.alert, styles.alertSuccess]}>\n            <Text style={styles.alertTitle}>✅ Todo en orden</Text>\n            <Text style={styles.alertText}>No hay pagos vencidos ni próximos a vencer</Text>\n          </View>\n        )}\n      </View>\n\n      {/* Acciones rápidas */}\n      <View style={styles.section}>\n        <Text style={styles.sectionTitle}>Acciones Rápidas</Text>\n        <View style={styles.actionsGrid}>\n          <TouchableOpacity\n            style={styles.actionButton}\n            onPress={() => navigation.navigate('Debtors')}\n          >\n            <Text style={styles.actionIcon}>👥</Text>\n            <Text style={styles.actionLabel}>Deudores</Text>\n          </TouchableOpacity>\n\n          <TouchableOpacity\n            style={styles.actionButton}\n            onPress={() => navigation.navigate('Payments')}\n          >\n            <Text style={styles.actionIcon}>💰</Text>\n            <Text style={styles.actionLabel}>Pagos</Text>\n          </TouchableOpacity>\n\n          <TouchableOpacity\n            style={styles.actionButton}\n            onPress={() => navigation.navigate('Debts')}\n          >\n            <Text style={styles.actionIcon}>📊</Text>\n            <Text style={styles.actionLabel}>Deudas</Text>\n          </TouchableOpacity>\n\n          <TouchableOpacity\n            style={styles.actionButton}\n            onPress={() => navigation.navigate('Reports')}\n          >\n            <Text style={styles.actionIcon}>📈</Text>\n            <Text style={styles.actionLabel}>Reportes</Text>\n          </TouchableOpacity>\n        </View>\n      </View>\n    </ScrollView>\n  );\n};\n\nconst styles = StyleSheet.create({\n  container: {\n    flex: 1,\n    backgroundColor: COLORS.background,\n  },\n  centerContainer: {\n    flex: 1,\n    justifyContent: 'center',\n    alignItems: 'center',\n    backgroundColor: COLORS.background,\n  },\n  header: {\n    paddingHorizontal: 20,\n    paddingTop: 20,\n    paddingBottom: 16,\n  },\n  greeting: {\n    fontSize: 24,\n    fontWeight: 'bold',\n    color: COLORS.text,\n  },\n  date: {\n    fontSize: 14,\n    color: COLORS.textSecondary,\n    marginTop: 4,\n  },\n  summaryCards: {\n    paddingHorizontal: 20,\n    gap: 12,\n    marginBottom: 24,\n  },\n  card: {\n    borderRadius: 12,\n    padding: 16,\n  },\n  cardPrimary: {\n    backgroundColor: COLORS.primary,\n  },\n  cardSecondary: {\n    backgroundColor: COLORS.secondary,\n  },\n  cardLabel: {\n    fontSize: 14,\n    color: '#fff',\n    opacity: 0.9,\n  },\n  cardValue: {\n    fontSize: 24,\n    fontWeight: 'bold',\n    color: '#fff',\n    marginTop: 8,\n  },\n  section: {\n    paddingHorizontal: 20,\n    marginBottom: 24,\n  },\n  sectionTitle: {\n    fontSize: 18,\n    fontWeight: '600',\n    color: COLORS.text,\n    marginBottom: 12,\n  },\n  alert: {\n    borderRadius: 8,\n    padding: 12,\n    marginBottom: 12,\n  },\n  alertDanger: {\n    backgroundColor: '#FFEBEE',\n    borderLeftWidth: 4,\n    borderLeftColor: COLORS.error,\n  },\n  alertWarning: {\n    backgroundColor: '#FFF3E0',\n    borderLeftWidth: 4,\n    borderLeftColor: COLORS.warning,\n  },\n  alertSuccess: {\n    backgroundColor: '#E8F5E9',\n    borderLeftWidth: 4,\n    borderLeftColor: COLORS.success,\n  },\n  alertTitle: {\n    fontSize: 14,\n    fontWeight: '600',\n    color: COLORS.text,\n  },\n  alertText: {\n    fontSize: 13,\n    color: COLORS.textSecondary,\n    marginTop: 4,\n  },\n  actionsGrid: {\n    flexDirection: 'row',\n    flexWrap: 'wrap',\n    gap: 12,\n  },\n  actionButton: {\n    flex: 1,\n    minWidth: '45%',\n    backgroundColor: COLORS.surface,\n    borderRadius: 12,\n    padding: 16,\n    alignItems: 'center',\n    borderWidth: 1,\n    borderColor: COLORS.border,\n  },\n  actionIcon: {\n    fontSize: 32,\n    marginBottom: 8,\n  },\n  actionLabel: {\n    fontSize: 13,\n    fontWeight: '500',\n    color: COLORS.text,\n    textAlign: 'center',\n  },\n});\n\nexport default DashboardScreen;\n
